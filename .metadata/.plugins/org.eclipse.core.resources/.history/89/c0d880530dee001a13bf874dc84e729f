package aplicacao;

import javax.persistence.EntityManager;
import javax.persistence.EntityManagerFactory;
import javax.persistence.Persistence;

import dominio.Pessoa;

public class Programa {

	public static void main(String[] args) {
		
		Pessoa p1 = new Pessoa(null, "Daniel Silva", "danielsila@gmail.com");
		Pessoa p2 = new Pessoa(null, "Maria Moreira", "maria@gmail.com"); 
		Pessoa p3 = new Pessoa(null, "Joao Almeida", "joao@gmail.com"); 
		
		EntityManagerFactory emf = Persistence.createEntityManagerFactory("exemplo-jpa");
		EntityManager em = emf.createEntityManager();
		
		em.persist(p1);
		em.persist(p2);
		em.persist(p2);
		
        System.out.println(p1); 
        System.out.println(p2); 
        System.out.println(p3); 


	}

	private static Object CreateManager() {
		// TODO Auto-generated method stub
		return null;
	}

}
